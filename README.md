# AutomationExercise.com – Selenium + TestNG + POM Automation

The framework automates key test scenarios against https://automationexercise.com/

## Test Cases

| ID | Scenario | Priority | Expected Result | Screenshot |
|---|---|---|---|---|
| TC01 | Verify home page loads successfully with all key sections | High | Home page loads with header, category/brand sidebar, featured products and footer visible. | Yes (Figure 01) |
| TC02 | Verify user can register a new account with valid details | High | 'ACCOUNT CREATED!' message is shown; after Continue, 'Logged in as <username>' appears. | Yes (Figure 02) |
| TC03 | Verify registration fails with an already-registered email | High | Error message 'Email Address already exist!' is displayed. | Yes (Figure 03) |
| TC04 | Verify registered user can log in with valid credentials | High | 'Logged in as <username>' is visible in the header after login. | Yes (Figure 04) |
| TC05 | Verify login fails with incorrect email/password | High | Error message 'Your email or password is incorrect!' is displayed. | Yes (Figure 05) |
| TC06 | Verify user can log out | Medium | User is redirected to the Login page after clicking Logout. | No |
| TC07 | Verify all products list and product detail page | Medium | Clicking 'View Product' shows name, category, price, availability, condition, brand. | No |
| TC08 | Verify user can search for a product by keyword | High | 'SEARCHED PRODUCTS' heading and matching products are displayed. | Yes (Figure 06) |
| TC09 | Verify user can add a product to the cart | High | Product appears in the cart with correct name, price and quantity. | Yes (Figure 07) |
| TC10 | Verify product quantity can be increased before adding to cart | Medium | Cart shows the exact quantity selected on the product detail page. | Yes (Figure 07) |
| TC11 | Verify user can remove a product from the cart | Medium | Product row disappears from the cart table after clicking the 'X' remove button. | No |
| TC12 | Verify category browsing via the left sidebar | Low | Category page displays a heading like 'WOMEN - DRESS PRODUCTS' with matching items. | No |
| TC13 | Verify brand browsing via the left sidebar | Low | Brand page displays only products belonging to the selected brand. | No |
| TC14 | Verify guest can register during checkout and place an order | High | Order confirmation message 'Your order has been placed successfully!' is shown. | No |
| TC15 | Verify a logged-in user can complete checkout | High | Order confirmation message is shown using the already-logged-in session. | No |

## Prerequisites
- Java 17+ (`java -version`)
- Maven 3.8+ (`mvn -version`)
- Google Chrome installed (WebDriverManager auto-downloads the matching ChromeDriver — needs internet on first run)

## Setup (IntelliJ / Eclipse)
Open this folder as a Maven project — the IDE detects `pom.xml` automatically.

## Setup (VS Code)
1. Install the "Extension Pack for Java" (includes Maven + Test Runner for Java support).
2. File → Open Folder → select this folder. Wait for Java/Maven to finish indexing (bottom-right progress).
3. Run tests from the terminal (see below), or use the "Run Test" CodeLens links that appear above each `@Test` method.

## Before your first run
Open `src/test/java/tests/LoginTests.java` and replace `VALID_EMAIL` / `VALID_PASSWORD`
with an account that actually exists. Easiest approach: run `RegistrationTests` once,
note the generated email from the console, and reuse it here.

## Run all tests
```bash
mvn clean test
```

## Run a single class
```bash
mvn test -Dtest=SearchTests
```

## Reports
- **ExtentReports**: `test-output/ExtentReport.html` (Rich HTML report with embedded screenshots)
- TestNG's default HTML report: `test-output/index.html`
- Logs: `logs/automation.log` (Generated by Log4j2)
- Screenshots of failed tests: `test-output/screenshots/`

## Debugging Challenge (Step 7)
`SignupLoginPage.java` contains the login button locator
`By.cssSelector("button[data-qa='login-button']")`. To reproduce the assignment's
debugging exercise:
1. Temporarily change it to an invalid value, e.g. `"button[data-qa='loginBtn']"`.
2. Run `mvn test -Dtest=LoginTests` and observe the `NoSuchElementException` / `TimeoutException`.
3. Capture a screenshot of the failure.
4. Revert the locator to the correct value, re-run, and capture the passing result.

## Notes on Locators
AutomationExercise.com is purpose-built for automation practice and documents stable
`data-qa` attributes on its key form elements, which this framework relies on. Always
**Inspect Element** on the live page before your final run in case the markup has
changed since this project was written.
